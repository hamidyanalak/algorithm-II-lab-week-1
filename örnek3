#include <stdio.h>

int main()

{

  char name[30];
  int i;
  int boyut=0;
  printf("Isim giriniz:");
  scanf("%s", name);
  for(i=0; name[i] != '\0'; i++)
    boyut++;
  printf("Girdiginiz isim: %s, %d karakterden olusmaktadir.");
  return 0;
  
}
